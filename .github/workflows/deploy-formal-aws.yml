name: Deploy formal AWS

on:
  push:
    branches:
      - develop
  workflow_dispatch:

concurrency:
  group: rally-formal-aws-deploy
  cancel-in-progress: false

permissions:
  contents: read

env:
  AWS_REGION: ap-southeast-2
  AWS_ROLE_ARN: arn:aws:iam::765332581489:role/vcmi-formal-github-actions-deploy
  DEPLOY_BUCKET: vcmi-dev-deploy-765332581489-ap-southeast-2
  DEPLOY_KEY: releases/formal/rally-formal-source.zip
  EC2_INSTANCE_ID: i-0cd72e60d642457ae

jobs:
  verify:
    name: Verify application
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: pnpm

      - name: Install web dependencies
        run: pnpm install --frozen-lockfile

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: apps/api/requirements-dev.txt

      - name: Install API dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install -r apps/api/requirements-dev.txt

      - name: Check web
        run: |
          pnpm lint:web
          pnpm format:check:web
          pnpm typecheck:web

      - name: Check API
        run: |
          pnpm lint:api
          pnpm format:check:api
          pnpm test:api

      - name: Check Docker configuration
        run: docker compose -f infra/docker/docker-compose.yml config --quiet

  deploy:
    name: Deploy to formal EC2
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Log GitHub OIDC subject
        shell: bash
        run: |
          set -euo pipefail
          response=$(curl -sS -H "Authorization: bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com")
          token=$(jq -r '.value' <<<"$response")
          payload=$(cut -d. -f2 <<<"$token" | tr '_-' '/+' | awk '{ while (length($0) % 4) $0=$0"="; print }' | base64 -d)
          echo "OIDC subject presented to AWS:"
          jq -r '.sub' <<<"$payload"
          jq '{sub, aud, repository, repository_id, repository_owner, repository_owner_id}' <<<"$payload"

      - name: Configure AWS credentials through OIDC
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: rally-formal-${{ github.run_id }}

      - name: Build source bundle
        shell: bash
        run: |
          set -euo pipefail
          bundle_dir="$RUNNER_TEMP/rally-bundle"
          archive_path="$RUNNER_TEMP/rally-formal-source.zip"
          mkdir -p "$bundle_dir"
          git archive "${GITHUB_SHA}" | tar -x -C "$bundle_dir"
          printf '%s\n' "${GITHUB_SHA}" > "$bundle_dir/.rally-release-sha"
          (
            cd "$bundle_dir"
            zip -qr "$archive_path" .
          )
          sha256sum "$archive_path"
          aws s3 cp "$archive_path" "s3://${DEPLOY_BUCKET}/${DEPLOY_KEY}" \
            --metadata "commit=${GITHUB_SHA},branch=develop"

      - name: Deploy with Systems Manager
        id: deploy
        shell: bash
        run: |
          set -euo pipefail

          echo "Checking EC2 instance ${EC2_INSTANCE_ID} SSM connectivity..."
          instance_state=$(aws ec2 describe-instances \
            --instance-ids "$EC2_INSTANCE_ID" \
            --query 'Reservations[0].Instances[0].State.Name' \
            --output text)
          echo "EC2 state: ${instance_state}"
          if [[ "$instance_state" != "running" ]]; then
            echo "Formal EC2 instance is not running (state=${instance_state})." >&2
            exit 1
          fi

          # IAM role only grants SendCommand/GetCommandInvocation — not
          # DescribeInstanceInformation — so probe with a short no-op command.
          ping_id=$(aws ssm send-command \
            --instance-ids "$EC2_INSTANCE_ID" \
            --document-name AWS-RunShellScript \
            --comment "Rally formal SSM ping ${GITHUB_SHA}" \
            --timeout-seconds 120 \
            --parameters '{"commands":["echo ssm-online"],"executionTimeout":["60"]}' \
            --query 'Command.CommandId' \
            --output text)
          echo "ssm_ping_command_id=${ping_id}"
          ping_status="Pending"
          for _ in $(seq 1 24); do
            ping_invocation=$(aws ssm get-command-invocation \
              --command-id "$ping_id" \
              --instance-id "$EC2_INSTANCE_ID" \
              --output json 2>/dev/null || true)
            ping_status=$(jq -r '.Status // "Pending"' <<<"$ping_invocation")
            case "$ping_status" in
              Success)
                echo "SSM agent is online."
                break
                ;;
              Failed|TimedOut|Cancelled|Cancelling)
                echo "SSM ping failed:" >&2
                jq '{Status, StatusDetails, StandardOutputContent, StandardErrorContent}' \
                  <<<"$ping_invocation" >&2
                exit 1
                ;;
              *)
                sleep 5
                ;;
            esac
          done
          if [[ "$ping_status" != "Success" ]]; then
            echo "SSM agent did not come online within 2 minutes (status=${ping_status})." >&2
            jq '{Status, StatusDetails, StandardOutputContent, StandardErrorContent}' \
              <<<"$ping_invocation" >&2 || true
            exit 1
          fi

          remote_script=$(cat <<EOF
          set -euo pipefail
          export RALLY_RELEASE_SHA='${GITHUB_SHA}'
          export RALLY_RELEASE_BRANCH='develop'
          export RALLY_SOURCE_BUCKET='${DEPLOY_BUCKET}'
          export RALLY_SOURCE_KEY='${DEPLOY_KEY}'
          [[ '${GITHUB_SHA}' =~ ^[0-9a-f]{40}$ ]] || exit 1
          archive="/tmp/rally-formal-${GITHUB_SHA}.zip"
          release="/opt/rally-releases/${GITHUB_SHA}"
          aws s3 cp "s3://${DEPLOY_BUCKET}/${DEPLOY_KEY}" "\$archive" --region '${AWS_REGION}' --only-show-errors
          rm -rf "\$release"
          mkdir -p "\$release"
          unzip -q "\$archive" -d "\$release"
          exec bash "\$release/scripts/aws/deploy-formal-ec2.sh"
          EOF
          )

          encoded_script=$(printf '%s' "$remote_script" | base64 -w 0)
          parameters=$(jq -n \
            --arg command "echo '$encoded_script' | base64 -d | bash" \
            '{commands: [$command], executionTimeout: ["1500"]}')
          command_id=$(aws ssm send-command \
            --instance-ids "$EC2_INSTANCE_ID" \
            --document-name AWS-RunShellScript \
            --comment "Deploy Rally formal ${GITHUB_SHA}" \
            --timeout-seconds 120 \
            --parameters "$parameters" \
            --query 'Command.CommandId' \
            --output text)

          echo "command_id=${command_id}"
          echo "command_id=$command_id" >> "$GITHUB_OUTPUT"

          status="Pending"
          for _ in $(seq 1 150); do
            invocation=$(aws ssm get-command-invocation \
              --command-id "$command_id" \
              --instance-id "$EC2_INSTANCE_ID" \
              --output json 2>/dev/null || true)
            status=$(jq -r '.Status // "Pending"' <<<"$invocation")
            case "$status" in
              Success)
                printf '%s\n' "$invocation" | jq -r '.StandardOutputContent'
                break
                ;;
              Failed|TimedOut|Cancelled|Cancelling)
                echo "SSM deploy command failed:" >&2
                jq '{Status, StatusDetails, ResponseCode, StandardOutputContent, StandardErrorContent}' \
                  <<<"$invocation" >&2
                exit 1
                ;;
              *)
                sleep 10
                ;;
            esac
          done

          if [[ "$status" != "Success" ]]; then
            echo "Deployment did not finish within 25 minutes. Last status: $status" >&2
            jq '{Status, StatusDetails, StandardOutputContent, StandardErrorContent}' \
              <<<"$invocation" >&2 || true
            aws ssm cancel-command \
              --command-id "$command_id" \
              --instance-ids "$EC2_INSTANCE_ID" \
              >/dev/null 2>&1 || true
            exit 1
          fi

      - name: Verify public endpoints and tag release
        shell: bash
        run: |
          set -euo pipefail
          public_ip=$(aws ec2 describe-instances \
            --instance-ids "$EC2_INSTANCE_ID" \
            --query 'Reservations[0].Instances[0].PublicIpAddress' \
            --output text)
          curl --fail --silent --show-error --retry 8 --retry-delay 5 \
            "http://${public_ip}:8000/health"
          curl --fail --silent --show-error --retry 8 --retry-delay 5 \
            --output /dev/null "http://${public_ip}:3000"
          aws ec2 create-tags \
            --resources "$EC2_INSTANCE_ID" \
            --tags \
              "Key=RallyCommit,Value=${GITHUB_SHA}" \
              "Key=RallyBranch,Value=develop" \
              "Key=RallyWebUrl,Value=http://${public_ip}:3000"
          echo "Rally formal deployed to http://${public_ip}:3000"
